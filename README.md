to run from scratch:-
1) sudo docker compose up -d --build
2) if MIH-Minio is not starting correctly run "sudo chown 1001 File_Storage/" in prject folder then run step 1
3) add access key to MIH-Minio
4) make dev bucket public
